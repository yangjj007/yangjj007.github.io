# Project pages

Each project page lives in its own self-contained folder under `projects/`.

```text
projects/
└── medgen/
    ├── index.html
    └── static/
```

To add another project, create `projects/<project-slug>/` and add a card and link in `_pages/about.md`.
